# JWT secret key - auto-generated if not set, but set one for persistence across restarts
JWT_SECRET=change-me-to-a-random-secret-key

# Optional: default Anthropic API key (users still enter their own key in the UI)
# ANTHROPIC_API_KEY=sk-ant-...
